var onFixedWidthTableResized = function(e)
{
    var table = $(e.currentTarget);
    var id = table.css("id");
    console.log("Processing table id ", id);
    // figure out how to pass columns widths to the encoded table
};
